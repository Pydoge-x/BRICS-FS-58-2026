# Ubuntu 安装 Docker 及免账号镜像加速

## 一、前置检查

```bash
# 内核版本需 >= 3.10
uname -r
```

---

## 二、卸载旧版本(可选)

```bash
sudo apt-get remove -y docker docker-engine docker.io containerd runc
```

---

## 三、安装依赖

```bash
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg lsb-release
```

---

## 四、添加阿里云 Docker CE 软件源

> 此处使用阿里云镜像站安装 Docker，不需要登录阿里云账号。

```bash
sudo install -m 0755 -d /etc/apt/keyrings

curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

sudo chmod a+r /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
  https://mirrors.aliyun.com/docker-ce/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt-get update
```

---

## 五、安装 Docker

```bash
sudo apt-get install -y docker-ce docker-ce-cli containerd.io \
  docker-buildx-plugin docker-compose-plugin
```

---

## 六、启动并设置开机自启

```bash
sudo systemctl enable --now docker
```

---

## 七、配置免账号镜像加速器

以下加速源无需登录，可直接使用。建议配置多个，避免单点故障。

```bash
sudo mkdir -p /etc/docker

sudo tee /etc/docker/daemon.json > /dev/null <<'EOF'
{
  "registry-mirrors": [
    "https://docker.xuanyuan.me",
    "https://docker.1ms.run",
    "https://hub.rat.dev",
    "https://docker.m.daocloud.io"
  ]
}
EOF

sudo systemctl daemon-reload
sudo systemctl restart docker
```

> 如果仍想使用阿里云加速器，可登录阿里云容器镜像服务控制台，获取专属地址后替换上面的数组内容。

---

## 八、验证

```bash
# 查看 Docker 版本
docker version

# 查看镜像加速器是否生效
docker info | grep -A 5 "Registry Mirrors"

# 拉取并运行测试镜像
docker pull hello-world
docker run --rm hello-world
```

---

## 九、免 sudo 使用 Docker（可选）

```bash
sudo usermod -aG docker $USER
newgrp docker
```

或重新登录终端后生效。

---

## 十、常用命令

| 操作 | 命令 |
|---|---|
| 查看镜像 | `docker images` |
| 查看运行容器 | `docker ps` |
| 查看所有容器 | `docker ps -a` |
| 停止容器 | `docker stop <容器ID>` |
| 删除容器 | `docker rm <容器ID>` |
| 删除镜像 | `docker rmi <镜像ID>` |
| 查看 Compose 版本 | `docker compose version` |
| 查看磁盘占用 | `docker system df` |
| 清理无用资源 | `docker system prune -a` |

---

## 十一、注意事项

1. 第三方加速源可能随时失效，如拉取失败可更换其他源。
2. 公共加速源仅建议用于拉取公开镜像，请勿传输私有或敏感镜像。
3. 修改 `/etc/docker/daemon.json` 时注意 JSON 格式，避免多余逗号导致 Docker 启动失败。
4. 修改配置后必须执行：

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
```

# Ubuntu 虚拟机 NAT 模式 SSH 连接指南

## 一、NAT 模式说明

NAT 模式下：

- 虚拟机可以访问外网。
- 宿主机不能直接通过虚拟机 IP 访问虚拟机。
- 需要在虚拟机软件中配置 **端口转发**，将宿主机端口映射到虚拟机 22 端口。
- 连接时使用 `127.0.0.1` 或 `localhost` 加映射端口。

---

## 二、Ubuntu 安装并启动 SSH

```bash
sudo apt update
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
sudo systemctl status ssh
```

## 三、获取虚拟机 IP

配置端口转发时需要用到虚拟机 IP。

```bash
ip addr
```

## 四、配置端口转发

### 1. VMware Workstation

1. 打开 VMware，点击菜单栏：**虚拟机** → **设置** → **网络适配器**。
2. 选择NAT模式
3. 点击确定

## 五、从宿主机连接（推荐使用VScode）

使用vscode自带的远程资源管理器进行连接。


# ROS2可能问题

## 一、在运行`install-ros2.sh`遇到安装报错提示：`未绑定的变量`类似错误

- 解决办法：将`install-ros2.sh`文件里面的首行`set -euo pipefail`修改为:`set -eo pipefail`


## 二、在共享文件夹中运行`colcon build --symlink-install`命令时提示报错

- 解决办法：不传入参数，直接运行`colcon build` 命令即可（比赛可以忽略这个问题应该）

# Dify 可能出现的问题

## 一、安装完成后，dify启动时提示`! Container config-db_p... Skipped: optional dependency "db_postgres" failed to start  3.1s`错误：

- 解决办法：可能时权限问题