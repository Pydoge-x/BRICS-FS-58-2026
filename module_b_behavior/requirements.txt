# ============================================
# 模块 B：人体行为识别 - 完整项目依赖
# Python: 3.10
# 生成时间: 2026-07-20（基于 venv 实际环境）
# ============================================
#
# 安装方式：
#   pip install -r requirements.txt
#   python scripts/setup_mmaction2.py --skip-pip   # 课外 MMAction2 验证
#
# PyTorch（CPU）：
#   pip install torch==2.0.1+cpu torchvision==0.15.2+cpu \
#     --index-url https://download.pytorch.org/whl/cpu
#
# PyTorch（CUDA 11.8）：
#   pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 \
#     --index-url https://download.pytorch.org/whl/cu118
#
# ============================================

# --- 核心计算 ---
torch==2.0.1                  # CPU: +cpu / CUDA: +cu118
torchvision==0.15.2
numpy>=1.24.0,<2.0.0          # 注意：必须 <2.0，兼容 torch 2.0.1 与 ultralytics
opencv-python>=4.6.0
opencv-contrib-python>=4.6.0

# --- YOLO 检测 + ByteTrack 追踪 ---
ultralytics>=8.0.0
lap>=0.5.12                   # ByteTrack 依赖

# --- 数据处理与可视化 ---
pillow>=9.5.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
scikit-image>=0.25.0
pandas>=2.0.0

# --- REST API + Web ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
httpx>=0.27.0                 # API 测试客户端

# --- 课外 MMAction2（CPU 推理） ---
openmim>=0.3.9
mmengine>=0.10.0
mmcv-lite>=2.0.0rc4,<2.2.0   # 注意：不能用 2.2.0，与 mmaction2 1.2.0 不兼容
mmaction2>=1.2.0
decord>=0.6.0                 # 视频解码
einops>=0.8.0
importlib_metadata>=6.0

# --- 通用工具 ---
pyyaml>=6.0
tqdm>=4.65.0

# --- 测试 ---
pytest>=7.0.0
