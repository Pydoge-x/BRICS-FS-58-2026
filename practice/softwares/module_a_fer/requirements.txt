# ============================================
# 模块 A：表情识别（FER）- 完整项目依赖
# Python: 3.10
# 生成时间: 2026-07-20（基于 venv 实际环境）
# ============================================
#
# 安装方式：
#   pip install -r requirements.txt
#
# PyTorch（CPU）：
#   pip install torch==2.0.1+cpu torchvision==0.15.2+cpu \
#     --index-url https://download.pytorch.org/whl/cpu
#
# PyTorch（CUDA 11.8）：
#   pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 \
#     --index-url https://download.pytorch.org/whl/cu118
#
# ============================================

# --- 核心计算 ---
numpy>=1.24.0,<2.0.0        # 注意：必须 <2.0，兼容 torch 2.0.1
pillow>=9.5.0

# --- PyTorch 相关 ---
torch==2.0.1                 # CPU: +cpu / CUDA: +cu118
torchvision==0.15.2

# --- 模型与推理 ---
timm>=0.9.0                  # MobileViT 等模型
onnx>=1.14.0
onnxruntime>=1.15.0
onnxruntime-gpu>=1.15.0      # GPU 推理加速
onnxsim>=0.4.0               # ONNX 模型简化

# --- 数据与图像处理 ---
opencv-python>=4.8.0
opencv-contrib-python>=5.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
scikit-image>=0.25.0
matplotlib>=3.7.0
seaborn>=0.12.0              # 训练可视化

# --- 人脸检测 ---
insightface>=0.7.3           # InsightFace 高精度人脸检测（推荐）
# mtcnn>=0.1.0               # MTCNN 人脸检测（备选）

# --- Web 服务 ---
flask>=2.3.0
flask-cors>=4.0.0

# --- 训练工具 ---
tqdm>=4.65.0
pyyaml>=6.0

# --- 开发测试（可选）---
pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
